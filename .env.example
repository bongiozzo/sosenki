# Environment Configuration Template
# Copy to .env and fill in actual values
# .env is used by both Makefile and VS Code launch.json
# WARNING: .env contains secrets - DO NOT commit to version control

# ============================================================================
# Environment Mode
# ============================================================================
# Set to 'dev' for local development, 'prod' for production
# This determines which database file is used and which commands are allowed
ENV=dev

# ============================================================================
# Database Configuration
# ============================================================================
# Dev: sosenki.dev.db (safe to reset/seed anytime)
# Prod: sosenki.db (only modified via restore or migrations)
DATABASE_URL=sqlite:///./sosenki.dev.db
GOOGLE_CREDENTIALS_PATH=.env_google_credentials.json
SEEDING_CONFIG_PATH=src/config/seeding.json
TELEGRAM_BOT_NAME=SG_SOSenki_Bot
TELEGRAM_MINI_APP_ID=sosenki-mini-app-id

# ============================================================================
# SECRETS - DO NOT COMMIT (fill in .env only, not in .env.example)
# ============================================================================
# Telegram Bot Token (secret)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Google Sheets ID (private data - canonical data source)
GOOGLE_SHEET_ID=your_google_sheet_id_here

# ============================================================================
# Dynamic Configuration (set by setup-environment.sh)
# ============================================================================
# MINI_APP_URL=set by setup-environment.sh (ngrok tunnel)
# WEBHOOK_URL=set by setup-environment.sh (ngrok tunnel)

